<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.muhammadusama.home_budget">

    <application
        android:allowBackup="true"
        android:icon="@drawable/piggy_bank"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".HomeActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name=".ChartLine" />
        <activity android:name=".ChartBar" />
        <activity android:name=".view_card" />
        <activity android:name=".Categories" />
        <activity android:name=".Settings_Activity" />
        <activity android:name=".Statistics_Activity" />
        <activity android:name=".Linechart_Activity" />
        <activity android:name=".Home" />
        <activity android:name=".IntroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>